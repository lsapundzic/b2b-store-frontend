"use client";

import { Button } from "@nextui-org/react";

export default function Navbar({ routes }) {
  console.log("Navbar running...");

  return (
    <nav>
      <Button>Click!</Button>
    </nav>
  );
}
